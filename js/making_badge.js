// function readUrl(event) {
//   const file = event.target.files[0];

//   const reader = new FileReader();

//   reader.onload = function (event) {
//     $("#making_badge_attach_image").src = event.target.result;
//   };

//   reader.readAsDataURL(file);
// }
